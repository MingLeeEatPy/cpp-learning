# C 语言学习笔记：为什么十进制转二进制要“除 2 取余”？

## 1. 先把二进制想成一种“装箱规则”

十进制每一位的单位是：

- 个位：1
- 十位：10
- 百位：100
- 千位：1000

例如：

`537 = 5×100 + 3×10 + 7×1`

二进制也是一样，只不过每一位的单位不是 1、10、100、1000，而是：

- 第 0 位：1
- 第 1 位：2
- 第 2 位：4
- 第 3 位：8
- 第 4 位：16
- ...

也就是 2 的幂。

例如：

`10110₂ = 1×16 + 0×8 + 1×4 + 1×2 + 0×1 = 22`

---

## 2. 为什么“除以 2”能够找到最后一位？

二进制的最后一位只有两种可能：

- `0`
- `1`

一个整数除以 2 时，余数也只有两种可能：

- 余数 `0`
- 余数 `1`

而且：

- 偶数除以 2，余数是 `0`
- 奇数除以 2，余数是 `1`

这正好对应二进制最后一位：

- 偶数的二进制一定以 `0` 结尾
- 奇数的二进制一定以 `1` 结尾

所以：

> **一个数除以 2 的余数，就是它的二进制最低位（最右边那一位）。**

---

## 3. 用 22 来看

我们想把十进制的 `22` 转成二进制。

### 第一次除以 2

`22 ÷ 2 = 11 …… 0`

余数是 `0`。

所以我们已经知道：

`22` 的二进制最后一位是 `0`。

目前可以想成：

`????0`

然后继续处理商 `11`。

### 第二次

`11 ÷ 2 = 5 …… 1`

得到下一位：`1`

### 第三次

`5 ÷ 2 = 2 …… 1`

得到下一位：`1`

### 第四次

`2 ÷ 2 = 1 …… 0`

得到下一位：`0`

### 第五次

`1 ÷ 2 = 0 …… 1`

得到下一位：`1`

得到的余数顺序是：

`0 1 1 0 1`

但我们是从**最右边最低位**开始找的，所以最后要倒过来：

`10110`

因此：

`22₁₀ = 10110₂`

---

## 4. 为什么要倒着写？

因为第一次除以 2 找到的是最后一位。

第二次找到的是倒数第二位。

第三次找到的是倒数第三位。

所以寻找顺序是：

`最低位 → 更高一位 → 更高一位 → ...`

但正常写数字时，我们是：

`最高位 → ... → 最低位`

因此需要把余数倒过来写。

---

## 5. 最容易理解的类比：十进制“除以 10 取余”

假设我们不知道 `5372` 的每一位，想从右往左把它拆出来。

`5372 ÷ 10 = 537 …… 2`

余数 `2` 就是个位。

然后：

`537 ÷ 10 = 53 …… 7`

余数 `7` 就是十位。

继续：

`53 ÷ 10 = 5 …… 3`

最后：

`5 ÷ 10 = 0 …… 5`

于是从右往左得到：

`2, 7, 3, 5`

倒过来就是：

`5372`

所以：

> 十进制中，“除以 10 取余”可以一位一位拆出数字。

同样：

> 二进制中，“除以 2 取余”可以一位一位拆出数字。

---

## 6. 真正应该记住的规律

不要只死记：

> 十进制转二进制 = 除 2 取余。

更重要的是理解：

> **几进制，就不断除以几。每次的余数就是当前最低位。**

例如：

- 转二进制：不断除以 `2`
- 转八进制：不断除以 `8`
- 转十六进制：不断除以 `16`

---

## 7. 和 C 语言有什么关系？

C 语言里，整数在计算机中本质上以二进制形式存储。

例如：

```c
int x = 22;
```

从人的角度看是十进制 `22`。

从计算机内部看，它最终对应的是一串二进制位，例如：

`00000000 00000000 00000000 00010110`

后面学习位运算时会遇到：

```c
x & 1
```

它可以判断二进制最后一位是 `0` 还是 `1`。

还会遇到：

```c
x >> 1
```

它相当于把二进制整体向右移动一位。

这和“除以 2、取余数”的思想高度相关。

---

## 8. 一句话总结

**十进制转二进制之所以能用“不断除以 2 取余”，是因为每次除以 2 的余数刚好告诉我们当前二进制的最后一位，而商则是去掉这一位以后剩下的部分。**

所以这个方法并不是一个需要硬背的技巧，而是二进制位权结构自然推出来的。
